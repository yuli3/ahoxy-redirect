# ahoxy.com — redirect shell. Crawling must stay ALLOWED so search engines
# can discover the 301s and transfer equity to the oiyo family.
User-agent: *
Allow: /

# The sitemap lists URLs that 301 — on purpose. This host answered Googlebot
# with 403 from ~2026-07-11 to 2026-08-13 (Bot Fight Mode), so none of these
# redirects were ever read. Listing the old URLs is Google's own site-move
# lever for getting them re-fetched. Entries will show as 'Page with redirect'
# in Search Console; that is the intended outcome. Retire once equity has moved.
Sitemap: https://ahoxy.com/sitemap.xml
